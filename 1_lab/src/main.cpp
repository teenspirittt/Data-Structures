#include "../headers/menu.h"

/* TODO :
  teenspirit list:
    copy constructor
    getLookedElemCount();
    editValue();
    CycList(copy);
    removeVal(by index)
    remove()

  badblues list:
    выбор типа данных в меню (при запуске программы? )
 */

int main() {
  MainMenu();
  return 0;
}