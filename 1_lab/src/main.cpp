#include "../headers/menu.h"

/* TODO :
  teenspirit list:
    copy constructor
    editValue();
    CycList(copy);
    removeVal(by index) + считать просмотренные элементы (l_elem_c)

  badblues list:
    iteratory ебучие
 */

int main() {
  MainMenu();
  return 0;
}